Create a new MindRoot component following project conventions:

Component name: $ARGUMENTS

Rules:
- Place in the appropriate `src/components/<module>/` directory
- Use default export for internal components
- Follow dark theme design system (bg #111318, surface #1a1d24, text #e8e0d4)
- Use Tailwind classes with project color tokens (light, mind, heart, soul, muted, bg, surface, border)
- Fonts: font-serif (headings), font-sans (body), font-mono (labels/badges)
- No emoji — use word-based labels
- TypeScript strict, proper typing for all props
- UI strings in Brazilian Portuguese, code/comments in English
